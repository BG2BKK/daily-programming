#include <iostream>
#include <algorithm>

using namespace std;

int main()
{

	// get args N 
	// get Stream from stdin
	//
	//
	// frist build a N-elements heap
	// fixup heap and convert it into minheap
	// second get NUM from stream
	// if NUM < top_of_heap then continue;
	// else recursively compare it with the smaller one of top_of_heap's two child
	//		if NUM < smaller_one then smaller_one'parent = NUM;
	//		else recursively

}
